found_blood_mage_coven_decision = {
	picture = "gfx/interface/illustrations/decisions/bm_decision_personal_religious.dds"
	major = yes
	ai_check_interval = 0
	desc = found_blood_mage_coven_decision_desc
	is_shown = {
		has_trait = blood_mage
		exists = house
		NOT = {
			house = {
				has_house_modifier = blood_mage_coven
			}
		}
	}
	is_valid = {
		is_house_head = yes

		# Want at least 10 blood mages in the family before
		house = {
			any_adult_house_member = {
				count >= 10
				has_trait = blood_mage
			}
		}
	}
	is_valid_showing_failures_only = {
	}
	effect = {
		found_blood_mage_coven_decision_effects = yes
	}
}

found_grand_blood_mage_coven_decision = {
	picture = "gfx/interface/illustrations/decisions/bm_decision_personal_religious.dds"
	major = yes
	ai_check_interval = 0
	desc = found_blood_mage_coven_decision_desc
	is_shown = {
		has_trait = blood_mage
		exists = house

		house = {
			has_house_modifier = blood_mage_coven
		}
	
	}
	is_valid = {
		is_house_head = yes

		# Want at least 100 blood mages in the family before
		house = {
			any_adult_house_member = {
				count >= 1
				has_trait = blood_mage
			}
		}
	}
	is_valid_showing_failures_only = {
	}
	effect = {
		found_grand_blood_mage_coven_decision_effects = yes
	}
}
