# Lifedraining
lifedrain_piety_cost_major = 50
lifedrain_piety_cost_major_neg = -50
lifedrain_piety_cost_minor = 25
lifedrain_piety_cost_minor_neg = -25

# Trait draining
trait_drain_base_piety_cost = -100
trait_drain_level_2_piety_cost = -75
trait_drain_level_3_piety_cost = -150
trait_drain_level_4_piety_cost = -200


# Modifiers, only 1-3 are used in the game, 4-10 are for mods
trait_drain_modifier_1 = 10
trait_drain_modifier_2 = 25
trait_drain_modifier_3 = 50
trait_drain_modifier_4 = 75



# Modifiers for the trait drain
trait_drain_easier_with_age = {
    value = age
    multiply = 2
}

trait_drain_harder_per_trait_modifier = {
    # Intellect
    if = {
        limit = { has_trait = intellect_good_1 }
        subtract = trait_drain_modifier_1
    }
    if = {
        limit = { has_trait = intellect_good_2 }
        subtract = trait_drain_modifier_2
    }
    if = {
        limit = { has_trait = intellect_good_3 }
        subtract = trait_drain_modifier_3
    }
    
    # Physique
    if = {
        limit = { has_trait = physique_good_1 }
        subtract = trait_drain_modifier_1
    }
    if = {
        limit = { has_trait = physique_good_2 }
        subtract = trait_drain_modifier_2
    }
    if = {
        limit = { has_trait = physique_good_3 }
        subtract = trait_drain_modifier_3
    }

    # Beauty
    if = {
        limit = { has_trait = beauty_good_1 }
        subtract = trait_drain_modifier_1
    }
    if = {
        limit = { has_trait = beauty_good_2 }
        subtract = trait_drain_modifier_2
    }
    if = {
        limit = { has_trait = beauty_good_3 }
        subtract = trait_drain_modifier_3
    }

    # Other traits
    if = {
        limit = { has_trait = fecund }
        subtract = trait_drain_modifier_1
    }
    if = {
        limit = { has_trait = giant }
        subtract = trait_drain_modifier_1
    }
    if = {
        limit = { has_trait = pure_blooded }
        subtract = trait_drain_modifier_1
    }

    # Blood of Numenor
    if = {
        limit = { 
            OR = {
                has_trait = blood_of_numenor_1
                has_trait = blood_of_numenor_2
                has_trait = blood_of_numenor_3
            }
        }
        subtract = trait_drain_modifier_1
    }
    if = {
        limit = { 
            OR = {
                has_trait = blood_of_numenor_4
                has_trait = blood_of_numenor_5
                has_trait = blood_of_numenor_6
            }
        }
        subtract = trait_drain_modifier_2
    }
    if = {
        limit = { 
            OR = {
                has_trait = blood_of_numenor_7
                has_trait = blood_of_numenor_8
                has_trait = blood_of_numenor_9
            }
        }
        subtract = trait_drain_modifier_3
    }
    if = {
        limit = { has_trait = blood_of_numenor_10 }
        subtract = trait_drain_modifier_4
    }
}


trait_drain_numenor_cost = {
    add = trait_drain_base_piety_cost
    if = {
        limit = { has_trait = blood_of_numenor_2 }
        add = trait_drain_level_2_piety_cost
    }
    if = {
        limit = { has_trait = blood_of_numenor_3 }
        add = trait_drain_level_3_piety_cost
    }
    if = {
        limit = { has_trait = blood_of_numenor_4 }
        add = trait_drain_level_4_piety_cost
    }
    if = {
        limit = { has_trait = blood_of_numenor_5 }
        add = trait_drain_level_5_piety_cost
    }
    if = {
        limit = { has_trait = blood_of_numenor_6 }
        add = trait_drain_level_6_piety_cost
    }
    if = {
        limit = { has_trait = blood_of_numenor_7 }
        add = trait_drain_level_7_piety_cost
    }
    if = {
        limit = { has_trait = blood_of_numenor_8 }
        add = trait_drain_level_8_piety_cost
    }
    if = {
        limit = { has_trait = blood_of_numenor_9 }
        add = trait_drain_level_9_piety_cost
    }
    if = {
        limit = { has_trait = blood_of_numenor_10 }
        add = trait_drain_level_10_piety_cost
    }
}


trait_drain_intellect_cost = {
    add = trait_drain_base_piety_cost
    if = {
        limit = { has_trait = intellect_good_2 }
        add = trait_drain_level_2_piety_cost
    }
    if = {
        limit = { has_trait = intellect_good_3 }
        add = trait_drain_level_3_piety_cost
    }
}

trait_drain_physique_cost = {
    add = trait_drain_base_piety_cost
    if = {
        limit = { has_trait = physique_good_2 }
        add = trait_drain_level_2_piety_cost
    }
    if = {
        limit = { has_trait = physique_good_3 }
        add = trait_drain_level_3_piety_cost
    }
}

trait_drain_beauty_cost = {
    value = trait_drain_base_piety_cost
    if = {
        limit = { has_trait = beauty_good_2 }
        add = trait_drain_level_2_piety_cost
    }
    if = {
        limit = { has_trait = beauty_good_3 }
        add = trait_drain_level_3_piety_cost
    }
}

trait_drain_modifier_per_trait = {
    value = 0

    # intellect
    if = {
        limit = { has_trait = intellect_good_1 }
        add = trait_drain_modifier_1
    }
    if = {
        limit = { has_trait = intellect_good_2 }
        add = trait_drain_modifier_2
    }
    if = {
        limit = { has_trait = intellect_good_3 }
        add = trait_drain_modifier_3
    }

    # Physique
    if = {
        limit = { has_trait = physique_good_1 }
        add = trait_drain_modifier_1
    }
    if = {
        limit = { has_trait = physique_good_2 }
        add = trait_drain_modifier_2
    }
    if = {
        limit = { has_trait = physique_good_3 }
        add = trait_drain_modifier_3
    }

    # Beauty
    if = {
        limit = { has_trait = beauty_good_1 }
        add = trait_drain_modifier_1
    }
    if = {
        limit = { has_trait = beauty_good_2 }
        add = trait_drain_modifier_2
    }
    if = {
        limit = { has_trait = beauty_good_3 }
        add = trait_drain_modifier_3
    }

    # Other traits
    if = {
        limit = { has_trait = giant }
        add = trait_drain_modifier_1
    }
    if = {
        limit = { has_trait = fecund }
        add = trait_drain_modifier_1
    }
    if = {
        limit = { has_trait = pure_blooded }
        add = trait_drain_modifier_1
    }

    # Blood of Numenor
    if = {
        limit = { 
            OR = {
                has_trait = blood_of_numenor_1
                has_trait = blood_of_numenor_2
                has_trait = blood_of_numenor_3
            }
        }
        add = trait_drain_modifier_1
    }
    if = {
        limit = { 
            OR = {
                has_trait = blood_of_numenor_4
                has_trait = blood_of_numenor_5
                has_trait = blood_of_numenor_6
            }
        }
        add = trait_drain_modifier_2
    }
    if = {
        limit = { 
            OR = {
                has_trait = blood_of_numenor_7
                has_trait = blood_of_numenor_8
                has_trait = blood_of_numenor_9
            }
        }
        add = trait_drain_modifier_3
    }
    if = {
        limit = { has_trait = blood_of_numenor_10 }
        add = trait_drain_modifier_4
    }
}